plugins {
    id 'java'
}

group 'no.tornado'
version '1.0.22'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes('Main-Class': 'fxlauncher.HeadlessMainLauncher',
                "Implementation-Version": project.version,
                "Class-Path": configurations.default.collect { "libs/${it.getName()}" }.join(' '))
    }
}

dependencies {

}
